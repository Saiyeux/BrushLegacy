# BrushLegacy dependencies
# Cross-platform: MacBook (M-series) + RT box (Ubuntu, Python 3.8, franka_new env)

# ── Core (both machines) ────────────────────────────────────────────────────
numpy>=1.24.0
opencv-python>=4.5.0
pillow>=9.0.0
pandas>=1.3.0
matplotlib>=3.5.0
scipy>=1.7.0
pyyaml>=6.0

# ── Inference only (MacBook) ────────────────────────────────────────────────
# Install manually on the inference machine:
#   pip install torch torchvision
# MPS (Apple Silicon) is auto-detected; no extra steps needed.

# ── Robot execution only (RT box) ───────────────────────────────────────────
# pyfranka is installed as an editable package — do not reinstall via pip.
# The franka_new conda env already contains all required packages.
